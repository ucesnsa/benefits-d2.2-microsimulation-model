# Copy this to .env and fill in your values.
# Read by uk/model/Code_FRS_23_24/utils/frs_config.py (via python-dotenv).
# Only needed for the optional Postgres-backed FRS pipeline steps; the tools
# and the verification suite never read it.
DB_USER=user
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dbname
